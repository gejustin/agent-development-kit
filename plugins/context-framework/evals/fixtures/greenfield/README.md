# widget-service

A small Node service that catalogs widgets and exposes them over HTTP.

```bash
yarn install
yarn dev
yarn test
```

## Layout

- `src/` — service source
- `test/` — Jest tests
- `migrations/` — database migrations (SQL)

This repo has no AI context yet. Use the context-framework plugin to author it.
